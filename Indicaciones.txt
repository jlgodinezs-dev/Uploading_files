Crear un directorio con el nombre de: uploads
#Crear virtual environment
python -m venv venv
#forzar políticas para activar el virtual environment
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Unrestricted -Force
venv\Scripts\actívate
#Instalar paquetes y dependencias
pip install -r requirements.txt
#Ejecutar API en el puerto 8000
uvicorn.exe main:app --host=127.0.0.1 --port=8000
